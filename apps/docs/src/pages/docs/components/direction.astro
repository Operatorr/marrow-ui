---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";
import { loadFull } from "../../../utils/template-loader";

const directionCode = loadFull("direction");
---

<DocsLayout title="Direction" description="Support for right-to-left (RTL) layouts using the dir attribute" activeComponent="direction">
  <div class="space-y-12">

    <!-- Hero -->
    <header>
      <div class="flex items-center gap-3 mb-4">
        <span class="docs-badge">Typography & Micro</span>
        <span class="docs-badge-primary docs-badge">Pure CSS</span>
      </div>
      <h1 class="docs-hero-title">Direction</h1>
      <p class="docs-hero-description mt-3">Support for right-to-left (RTL) layouts using the dir attribute.</p>
    </header>

    <!-- Preview -->
    <section>
      <span class="docs-section-label">Preview</span>
      <div class="docs-stage space-y-8">
        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">LTR (Left-to-Right)</p>
          <div class="max-w-sm">
            <div class="mw-card" dir="ltr">
              <div class="flex items-center gap-3 mb-3">
                <div class="mw-avatar">LR</div>
                <div class="flex-1">
                  <h4 class="font-heading font-semibold text-sm">Left-to-Right Layout</h4>
                  <p class="text-xs text-muted-foreground">Default direction for Western languages</p>
                </div>
              </div>
              <div class="space-y-3">
                <input type="text" class="mw-input" placeholder="Enter your name" />
                <button class="mw-btn mw-btn-primary w-full">Submit</button>
              </div>
              <p class="text-sm text-muted-foreground mt-3">
                Content flows from left to right, which is the default for most Western languages like English, French, and Spanish.
              </p>
            </div>
          </div>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">RTL (Right-to-Left)</p>
          <div class="max-w-sm">
            <div class="mw-card" dir="rtl">
              <div class="flex items-center gap-3 mb-3">
                <div class="mw-avatar">RL</div>
                <div class="flex-1">
                  <h4 class="font-heading font-semibold text-sm">من اليمين إلى اليسار</h4>
                  <p class="text-xs text-muted-foreground">الاتجاه الافتراضي للغات الشرقية</p>
                </div>
              </div>
              <div class="space-y-3">
                <input type="text" class="mw-input" placeholder="أدخل اسمك" />
                <button class="mw-btn mw-btn-primary w-full">إرسال</button>
              </div>
              <p class="text-sm text-muted-foreground mt-3">
                المحتوى يتدفق من اليمين إلى اليسار، وهو الاتجاه المستخدم في اللغات مثل العربية والعبرية والفارسية.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- In Context -->
    <section>
      <span class="docs-section-label">In Context</span>
      <div class="docs-stage">
        <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Multi-Language Settings Panel</p>
        <div class="rounded-lg border border-border overflow-hidden max-w-lg mx-auto">
          <div class="p-4 border-b border-border">
            <h3 class="font-heading font-semibold text-sm">Language & Direction Settings</h3>
            <p class="text-xs text-muted-foreground mt-1">Configure text direction for your application</p>
          </div>
          <div class="p-4 space-y-4">
            <!-- LTR Example -->
            <div class="rounded-lg border border-border p-3" dir="ltr">
              <div class="flex items-center justify-between mb-2">
                <div class="flex items-center gap-2">
                  <span class="mw-badge mw-badge-secondary">EN</span>
                  <span class="text-sm font-medium">English</span>
                </div>
                <span class="text-xs text-muted-foreground">LTR</span>
              </div>
              <p class="text-xs text-muted-foreground">This text is displayed left-to-right, which is the standard direction for English and most European languages.</p>
              <div class="mt-2">
                <input type="text" class="mw-input text-sm" placeholder="Search settings..." />
              </div>
            </div>

            <!-- RTL Example -->
            <div class="rounded-lg border border-border p-3" dir="rtl">
              <div class="flex items-center justify-between mb-2">
                <div class="flex items-center gap-2">
                  <span class="mw-badge mw-badge-secondary">AR</span>
                  <span class="text-sm font-medium">العربية</span>
                </div>
                <span class="text-xs text-muted-foreground">RTL</span>
              </div>
              <p class="text-xs text-muted-foreground">يتم عرض هذا النص من اليمين إلى اليسار، وهو الاتجاه المعتمد للغة العربية.</p>
              <div class="mt-2">
                <input type="text" class="mw-input text-sm" placeholder="البحث في الإعدادات..." />
              </div>
            </div>

            <!-- Hebrew RTL Example -->
            <div class="rounded-lg border border-border p-3" dir="rtl">
              <div class="flex items-center justify-between mb-2">
                <div class="flex items-center gap-2">
                  <span class="mw-badge mw-badge-secondary">HE</span>
                  <span class="text-sm font-medium">עברית</span>
                </div>
                <span class="text-xs text-muted-foreground">RTL</span>
              </div>
              <p class="text-xs text-muted-foreground">טקסט זה מוצג מימין לשמאל, שהוא הכיוון הסטנדרטי לעברית.</p>
              <div class="mt-2">
                <input type="text" class="mw-input text-sm" placeholder="חפש הגדרות..." />
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Code -->
    <section>
      <span class="docs-section-label">Code</span>
      <div class="space-y-4">
        <CodeBlock code={directionCode} lang="html" />
      </div>
    </section>

    <!-- API Reference -->
    <section>
      <span class="docs-section-label">API Reference</span>
      <div class="overflow-x-auto rounded-lg border border-border">
        <table class="docs-api-table">
          <thead><tr><th>Class</th><th>Description</th></tr></thead>
          <tbody>
            <tr>
              <td><code>dir="ltr"</code></td>
              <td>Sets left-to-right text direction (default for most languages)</td>
            </tr>
            <tr>
              <td><code>dir="rtl"</code></td>
              <td>Sets right-to-left text direction for Arabic, Hebrew, Persian, etc.</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <!-- Guidelines -->
    <section>
      <span class="docs-section-label">Guidelines</span>
      <div class="space-y-4">
        <div class="docs-note">
          <p><strong>Accessibility</strong></p>
          <ul class="list-disc list-inside mt-2 space-y-1">
            <li>Setting the correct text direction is essential for accessibility and internationalization</li>
            <li>Screen readers and browsers rely on the <code>dir</code> attribute to properly render and announce content</li>
            <li>For a complete RTL experience, set <code>dir="rtl"</code> on the <code>&lt;html&gt;</code> element</li>
          </ul>
        </div>
        <div class="docs-note">
          <p><strong>Best Practices</strong></p>
          <ul class="list-disc list-inside mt-2 space-y-1">
            <li>Use the <code>dir</code> attribute on any HTML element to control text direction</li>
            <li>All Marrow UI components automatically adapt to RTL layouts</li>
            <li>Flexbox, grid, padding, margin, and border styles flip appropriately when <code>dir="rtl"</code> is set</li>
            <li>Set <code>dir="rtl"</code> on the <code>&lt;html&gt;</code> element for page-wide RTL layout</li>
          </ul>
        </div>
      </div>
    </section>

  </div>
</DocsLayout>
