---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";

const directionCode = `<!-- LTR (default) -->
<div class="mw-card" dir="ltr">
  <div class="flex items-center gap-3 mb-2">
    <div class="mw-avatar">LR</div>
    <div>
      <h4 class="font-heading font-semibold text-sm">Left-to-Right</h4>
      <p class="text-xs text-muted-foreground">This card uses the default LTR direction.</p>
    </div>
  </div>
  <p class="text-sm text-muted-foreground">
    Content flows from left to right.
  </p>
</div>

<!-- RTL -->
<div class="mw-card" dir="rtl">
  <div class="flex items-center gap-3 mb-2">
    <div class="mw-avatar">RL</div>
    <div>
      <h4 class="font-heading font-semibold text-sm">من اليمين إلى اليسار</h4>
      <p class="text-xs text-muted-foreground">هذه البطاقة تستخدم اتجاه RTL.</p>
    </div>
  </div>
  <p class="text-sm text-muted-foreground">
    المحتوى يتدفق من اليمين إلى اليسار.
  </p>
</div>

<!-- Apply RTL to the entire page -->
<html dir="rtl">
  ...
</html>`;
---

<DocsLayout title="Direction" description="Support for right-to-left (RTL) layouts using the dir attribute" activeComponent="direction">
  <div class="space-y-8">
    <div>
      <h1 class="font-heading text-3xl font-bold tracking-tight text-pretty">Direction</h1>
      <p class="mt-2 text-muted-foreground leading-relaxed">Support for right-to-left (RTL) layouts using the dir attribute.</p>
    </div>

    <div class="mw-separator"></div>

    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight text-pretty">Preview</h2>
      <div class="mw-card">
        <div class="grid md:grid-cols-2 gap-6">
          <!-- LTR Example -->
          <div class="space-y-3">
            <h3 class="font-heading text-lg font-semibold">LTR (Left-to-Right)</h3>
            <div class="mw-card" dir="ltr">
              <div class="flex items-center gap-3 mb-3">
                <div class="mw-avatar">LR</div>
                <div class="flex-1">
                  <h4 class="font-heading font-semibold text-sm">Left-to-Right Layout</h4>
                  <p class="text-xs text-muted-foreground">Default direction for Western languages</p>
                </div>
              </div>
              <div class="space-y-3">
                <input type="text" class="mw-input" placeholder="Enter your name" />
                <button class="mw-btn mw-btn-primary w-full">Submit</button>
              </div>
              <p class="text-sm text-muted-foreground mt-3">
                Content flows from left to right, which is the default for most Western languages like English, French, and Spanish.
              </p>
            </div>
          </div>

          <!-- RTL Example -->
          <div class="space-y-3">
            <h3 class="font-heading text-lg font-semibold">RTL (Right-to-Left)</h3>
            <div class="mw-card" dir="rtl">
              <div class="flex items-center gap-3 mb-3">
                <div class="mw-avatar">RL</div>
                <div class="flex-1">
                  <h4 class="font-heading font-semibold text-sm">من اليمين إلى اليسار</h4>
                  <p class="text-xs text-muted-foreground">الاتجاه الافتراضي للغات الشرقية</p>
                </div>
              </div>
              <div class="space-y-3">
                <input type="text" class="mw-input" placeholder="أدخل اسمك" />
                <button class="mw-btn mw-btn-primary w-full">إرسال</button>
              </div>
              <p class="text-sm text-muted-foreground mt-3">
                المحتوى يتدفق من اليمين إلى اليسار، وهو الاتجاه المستخدم في اللغات مثل العربية والعبرية والفارسية.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight text-pretty">Code</h2>
      <CodeBlock code={directionCode} lang="html" />
    </div>

    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight text-pretty">Usage</h2>
      <div class="space-y-4">
        <div>
          <h3 class="font-heading text-lg font-semibold mb-2">Setting Direction</h3>
          <p class="text-muted-foreground leading-relaxed">
            Use the <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">dir</code> attribute on any HTML element to control text direction. Set it to <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">ltr</code> for left-to-right or <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">rtl</code> for right-to-left layouts.
          </p>
        </div>

        <div>
          <h3 class="font-heading text-lg font-semibold mb-2">Component Compatibility</h3>
          <p class="text-muted-foreground leading-relaxed">
            All Marrow UI components automatically adapt to RTL layouts. Flexbox, grid, padding, margin, and border styles will flip appropriately when <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">dir="rtl"</code> is set on a parent element.
          </p>
        </div>

        <div>
          <h3 class="font-heading text-lg font-semibold mb-2">Accessibility</h3>
          <p class="text-muted-foreground leading-relaxed">
            Setting the correct text direction is essential for accessibility and internationalization. Screen readers and browsers rely on the <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">dir</code> attribute to properly render and announce content in languages that use right-to-left scripts.
          </p>
        </div>

        <div>
          <h3 class="font-heading text-lg font-semibold mb-2">Page-Wide RTL</h3>
          <p class="text-muted-foreground leading-relaxed">
            For a complete RTL experience, set <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">dir="rtl"</code> on the <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">&lt;html&gt;</code> element. This will apply RTL layout to the entire page, including all components and text content.
          </p>
        </div>
      </div>
    </div>
  </div>
</DocsLayout>
