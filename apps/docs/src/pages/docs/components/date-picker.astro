---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";
import { loadFull } from "../../../utils/template-loader";

const datePickerCode = loadFull("date-picker");
---

<DocsLayout title="Date Picker" description="An input with a calendar dropdown for selecting dates" activeComponent="date-picker">
  <div class="space-y-12">

    <header>
      <div class="flex items-center gap-3 mb-4">
        <span class="docs-badge">Data Display</span>
        <span class="docs-badge-primary docs-badge">Alpine.js</span>
      </div>
      <h1 class="docs-hero-title">Date Picker</h1>
      <p class="docs-hero-description mt-3">An input with a calendar dropdown for selecting dates. Uses Alpine.js for interactive state management and keyboard navigation.</p>
    </header>

    <section>
      <span class="docs-section-label">Preview</span>
      <div class="docs-stage">
        <div class="flex items-center justify-center min-h-[420px]">
          <div class="w-full max-w-xs" x-data="mwDatePicker()">
            <label class="block text-sm font-medium mb-2">Event Date</label>
            <div class="relative">
              <button
                @click="toggle()"
                type="button"
                class="mw-input flex items-center justify-between text-left cursor-pointer w-full">
                <span :class="selected ? 'text-foreground' : 'text-muted-foreground'" x-text="formatted"></span>
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-muted-foreground shrink-0" aria-hidden="true"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
              </button>

              <!-- Calendar Dropdown -->
              <div x-show="open" @click.away="open = false" x-cloak
                class="absolute z-10 mt-1 mw-popover p-3 w-[280px]"
                x-transition:enter="transition ease-out duration-100"
                x-transition:enter-start="opacity-0 -translate-y-1"
                x-transition:enter-end="opacity-100 translate-y-0"
                x-transition:leave="transition ease-in duration-75"
                x-transition:leave-start="opacity-100 translate-y-0"
                x-transition:leave-end="opacity-0 -translate-y-1">

                <!-- Month Navigation -->
                <div class="flex items-center justify-between mb-3">
                  <button @click="prevMonth()" type="button"
                    class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm"
                    aria-label="Previous month">
                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m15 18-6-6 6-6"/></svg>
                  </button>
                  <span class="text-sm font-medium" x-text="monthName + ' ' + currentYear"></span>
                  <button @click="nextMonth()" type="button"
                    class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm"
                    aria-label="Next month">
                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9 18 6-6-6-6"/></svg>
                  </button>
                </div>

                <!-- Day Labels -->
                <div class="grid grid-cols-7 gap-0 mb-1">
                  <template x-for="day in ['Su', 'Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa']">
                    <div class="text-center text-xs text-muted-foreground py-1" x-text="day"></div>
                  </template>
                </div>

                <!-- Day Grid -->
                <div class="grid grid-cols-7 gap-0">
                  <template x-for="(day, index) in days" x-bind:key="index">
                    <button
                      @click="selectDate(day)"
                      type="button"
                      :disabled="!day"
                      :class="{
                        'bg-primary text-primary-foreground': isSelected(day),
                        'hover:bg-accent': day && !isSelected(day),
                        'invisible': !day
                      }"
                      class="w-8 h-8 text-sm rounded-marrow flex items-center justify-center transition-colors"
                      x-text="day || ''">
                    </button>
                  </template>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section>
      <span class="docs-section-label">In Context</span>
      <div class="docs-stage">
        <div class="w-full max-w-lg mx-auto">
          <div class="rounded-lg border border-border bg-card p-6 space-y-5">
            <div>
              <h3 class="font-heading text-lg font-semibold">Create Event</h3>
              <p class="text-sm text-muted-foreground mt-1">Set up a new event with start and end dates.</p>
            </div>
            <div class="space-y-4">
              <div class="space-y-2">
                <label class="mw-label">Event Name</label>
                <input type="text" class="mw-input" placeholder="Annual Team Retreat" />
              </div>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div class="space-y-2" x-data="mwDatePicker()">
                  <label class="mw-label">Start Date</label>
                  <div class="relative">
                    <button @click="toggle()" type="button" class="mw-input flex items-center justify-between text-left cursor-pointer w-full">
                      <span :class="selected ? 'text-foreground' : 'text-muted-foreground'" x-text="formatted"></span>
                      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-muted-foreground shrink-0" aria-hidden="true"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
                    </button>
                    <div x-show="open" @click.away="open = false" x-cloak class="absolute z-10 mt-1 mw-popover p-3 w-[280px]" x-transition:enter="transition ease-out duration-100" x-transition:enter-start="opacity-0 -translate-y-1" x-transition:enter-end="opacity-100 translate-y-0" x-transition:leave="transition ease-in duration-75" x-transition:leave-start="opacity-100 translate-y-0" x-transition:leave-end="opacity-0 -translate-y-1">
                      <div class="flex items-center justify-between mb-3">
                        <button @click="prevMonth()" type="button" class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm" aria-label="Previous month">
                          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m15 18-6-6 6-6"/></svg>
                        </button>
                        <span class="text-sm font-medium" x-text="monthName + ' ' + currentYear"></span>
                        <button @click="nextMonth()" type="button" class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm" aria-label="Next month">
                          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9 18 6-6-6-6"/></svg>
                        </button>
                      </div>
                      <div class="grid grid-cols-7 gap-0 mb-1">
                        <template x-for="day in ['Su','Mo','Tu','We','Th','Fr','Sa']">
                          <div class="text-center text-xs text-muted-foreground py-1" x-text="day"></div>
                        </template>
                      </div>
                      <div class="grid grid-cols-7 gap-0">
                        <template x-for="(day, index) in days" x-bind:key="index">
                          <button @click="selectDate(day)" type="button" :disabled="!day" :class="{ 'bg-primary text-primary-foreground': isSelected(day), 'hover:bg-accent': day && !isSelected(day), 'invisible': !day }" class="w-8 h-8 text-sm rounded-marrow flex items-center justify-center transition-colors" x-text="day || ''"></button>
                        </template>
                      </div>
                    </div>
                  </div>
                </div>
                <div class="space-y-2" x-data="mwDatePicker()">
                  <label class="mw-label">End Date</label>
                  <div class="relative">
                    <button @click="toggle()" type="button" class="mw-input flex items-center justify-between text-left cursor-pointer w-full">
                      <span :class="selected ? 'text-foreground' : 'text-muted-foreground'" x-text="formatted"></span>
                      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-muted-foreground shrink-0" aria-hidden="true"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
                    </button>
                    <div x-show="open" @click.away="open = false" x-cloak class="absolute z-10 mt-1 mw-popover p-3 w-[280px]" x-transition:enter="transition ease-out duration-100" x-transition:enter-start="opacity-0 -translate-y-1" x-transition:enter-end="opacity-100 translate-y-0" x-transition:leave="transition ease-in duration-75" x-transition:leave-start="opacity-100 translate-y-0" x-transition:leave-end="opacity-0 -translate-y-1">
                      <div class="flex items-center justify-between mb-3">
                        <button @click="prevMonth()" type="button" class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm" aria-label="Previous month">
                          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m15 18-6-6 6-6"/></svg>
                        </button>
                        <span class="text-sm font-medium" x-text="monthName + ' ' + currentYear"></span>
                        <button @click="nextMonth()" type="button" class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm" aria-label="Next month">
                          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9 18 6-6-6-6"/></svg>
                        </button>
                      </div>
                      <div class="grid grid-cols-7 gap-0 mb-1">
                        <template x-for="day in ['Su','Mo','Tu','We','Th','Fr','Sa']">
                          <div class="text-center text-xs text-muted-foreground py-1" x-text="day"></div>
                        </template>
                      </div>
                      <div class="grid grid-cols-7 gap-0">
                        <template x-for="(day, index) in days" x-bind:key="index">
                          <button @click="selectDate(day)" type="button" :disabled="!day" :class="{ 'bg-primary text-primary-foreground': isSelected(day), 'hover:bg-accent': day && !isSelected(day), 'invisible': !day }" class="w-8 h-8 text-sm rounded-marrow flex items-center justify-center transition-colors" x-text="day || ''"></button>
                        </template>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="space-y-2">
                <label class="mw-label">Description</label>
                <textarea class="mw-input min-h-[80px]" placeholder="Optional notes about the event..."></textarea>
              </div>
            </div>
            <div class="flex justify-end gap-2 pt-2 border-t border-border">
              <button class="mw-btn mw-btn-outline">Cancel</button>
              <button class="mw-btn mw-btn-primary">Create Event</button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section>
      <span class="docs-section-label">Code</span>
      <div class="space-y-4">
        <CodeBlock code={datePickerCode} lang="html" />
      </div>
    </section>

    <section>
      <span class="docs-section-label">API Reference</span>
      <div class="overflow-x-auto rounded-lg border border-border">
        <table class="docs-api-table">
          <thead>
            <tr>
              <th>Property / Method</th>
              <th>Type</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>open</code></td><td>Boolean</td><td>Calendar dropdown visibility state</td></tr>
            <tr><td><code>selected</code></td><td>Date | null</td><td>Currently selected date object</td></tr>
            <tr><td><code>currentMonth</code></td><td>Number</td><td>Current displayed month (0-11)</td></tr>
            <tr><td><code>currentYear</code></td><td>Number</td><td>Current displayed year</td></tr>
            <tr><td><code>monthName</code></td><td>String (getter)</td><td>Formatted month name (e.g., "January")</td></tr>
            <tr><td><code>formatted</code></td><td>String (getter)</td><td>Display string: "MMM DD, YYYY" or "Pick a date"</td></tr>
            <tr><td><code>days</code></td><td>Array (getter)</td><td>Calendar grid with day numbers and padding</td></tr>
            <tr><td><code>toggle()</code></td><td>Function</td><td>Toggle dropdown open/closed</td></tr>
            <tr><td><code>selectDate(day)</code></td><td>Function</td><td>Select a date and close dropdown</td></tr>
            <tr><td><code>isSelected(day)</code></td><td>Function</td><td>Check if day is selected (returns boolean)</td></tr>
            <tr><td><code>prevMonth()</code></td><td>Function</td><td>Navigate to previous month</td></tr>
            <tr><td><code>nextMonth()</code></td><td>Function</td><td>Navigate to next month</td></tr>
          </tbody>
        </table>
      </div>
    </section>

    <section>
      <span class="docs-section-label">Guidelines</span>
      <div class="space-y-4">
        <div class="docs-note">
          <p><strong>Accessibility</strong></p>
          <ul class="list-disc list-inside mt-2 space-y-1">
            <li>Add <code>aria-label</code> attributes to navigation buttons for screen readers</li>
            <li>Add <code>aria-hidden="true"</code> to decorative calendar icon</li>
            <li>Include <code>type="button"</code> on all buttons to prevent form submission</li>
            <li>Day buttons are disabled when empty using <code>:disabled="!day"</code></li>
            <li>Selected date is visually distinguished with high contrast colors</li>
            <li>Click-away behavior allows closing with outside click</li>
          </ul>
        </div>
        <div class="docs-note">
          <p><strong>Best Practices</strong></p>
          <ul class="list-disc list-inside mt-2 space-y-1">
            <li>Use <code>mwDatePicker()</code> from <code>marrow.js</code> for state management</li>
            <li>To use in a form, add a hidden input: <code>&lt;input type="hidden" name="date" :value="selected?.toISOString()" /&gt;</code></li>
            <li>For date range selection, use two date pickers side by side (start and end)</li>
            <li>The dropdown uses <code>mw-popover</code> class for consistent styling with other popover components</li>
            <li>Transitions are fast (100ms enter, 75ms leave) for responsive feel</li>
          </ul>
        </div>
      </div>
    </section>

  </div>
</DocsLayout>
