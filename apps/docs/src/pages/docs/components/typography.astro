---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";
import { loadTemplate } from "../../../utils/template-loader";

const t = loadTemplate("typography");
const headingsCode = t.sections.get("Headings")!;
const paragraphCode = t.sections.get("Paragraph")!;
const inlineCode = t.sections.get("Inline")!;
const blockquoteCode = t.sections.get("Blockquote")!;
const listsCode = t.sections.get("Lists")!;
---

<DocsLayout title="Typography" description="Type scale, headings, paragraphs, inline elements, and text utilities" activeComponent="typography">
  <div class="space-y-12">

    <header>
      <div class="flex items-center gap-3 mb-4">
        <span class="docs-badge">Typography & Micro</span>
        <span class="docs-badge-primary docs-badge">Pure CSS</span>
      </div>
      <h1 class="docs-hero-title">Typography</h1>
      <p class="docs-hero-description mt-3">A complete typography system built on CSS custom properties. Provides a semantic type scale, dual font system (heading + body), and utility classes for all text styling needs.</p>
    </header>

    <section>
      <span class="docs-section-label">Preview</span>
      <div class="docs-stage space-y-8">
        <!-- Heading Scale -->
        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Heading 1 &mdash; 4xl/5xl Extrabold</p>
          <h1 class="font-heading text-4xl font-extrabold tracking-tight lg:text-5xl">
            The Future of Web Development
          </h1>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Heading 2 &mdash; 3xl Semibold</p>
          <h2 class="font-heading text-3xl font-semibold tracking-tight border-b pb-2">
            Building Modern Interfaces
          </h2>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Heading 3 &mdash; 2xl Semibold</p>
          <h3 class="font-heading text-2xl font-semibold tracking-tight">
            Core Design Principles
          </h3>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Heading 4 &mdash; xl Semibold</p>
          <h4 class="font-heading text-xl font-semibold tracking-tight">
            Getting Started with Marrow
          </h4>
        </div>

        <!-- Paragraph Styles -->
        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Lead Paragraph &mdash; xl Muted</p>
          <p class="text-xl text-muted-foreground leading-relaxed">
            This is a lead paragraph that introduces your content. It uses a larger font size and muted color
            to serve as a bridge between headings and body text, perfect for article introductions.
          </p>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Standard Paragraph &mdash; Base Size</p>
          <p class="leading-7">
            The standard paragraph style uses the body font with a comfortable line height optimized for
            readability. This is the default text style you'll use for most content. It balances density
            and breathing room to keep readers engaged without overwhelming them.
          </p>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Large Text &mdash; lg Size</p>
          <p class="text-lg leading-7">
            Slightly larger text for emphasis without reaching for a full heading. Useful for callouts
            or important statements within your content.
          </p>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Small Text &mdash; sm Muted</p>
          <p class="text-sm text-muted-foreground">
            Small, muted text for secondary information like timestamps, captions, helper text below
            form fields, or legal disclaimers.
          </p>
        </div>

        <!-- Inline Elements -->
        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Bold & Italic</p>
          <p class="leading-7">
            You can use <strong>bold text</strong> for strong emphasis and <em>italic text</em>
            for subtle highlighting or to indicate technical terms, foreign words, or thoughts.
          </p>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Inline Code</p>
          <p class="leading-7">
            Install the package with <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">npm install marrow-ui</code>
            and use the <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">mw-btn</code> class on any
            <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">&lt;button&gt;</code> element to apply button styles.
          </p>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Links</p>
          <p class="leading-7">
            Check out the <a href="/docs/installation" class="text-primary hover:underline">installation guide</a>
            to get started, or browse the <a href="/docs/components/button" class="text-primary hover:underline">component library</a>
            for examples and code snippets.
          </p>
        </div>

        <!-- Blockquote -->
        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Blockquote</p>
          <blockquote class="border-l-4 border-primary pl-4 italic text-muted-foreground">
            "The details are not the details. They make the design. The connections, the connections,
            the connections. It will in the end be these details that give the product its life."
            <footer class="mt-2 text-sm not-italic">&mdash; Charles Eames</footer>
          </blockquote>
        </div>

        <!-- Lists -->
        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Unordered List</p>
          <ul class="list-disc pl-6 space-y-1 leading-7">
            <li>Utility-first CSS powered by Tailwind</li>
            <li>Alpine.js for lightweight interactivity</li>
            <li>Fully customizable theme system via config</li>
            <li>No complex build process or bundler required</li>
            <li>Copy-paste components ready to use</li>
          </ul>
        </div>

        <div>
          <p class="text-xs font-semibold uppercase tracking-wider text-muted-foreground/70 mb-3">Ordered List</p>
          <ol class="list-decimal pl-6 space-y-1 leading-7">
            <li>Install Marrow UI via npm or script tag</li>
            <li>Configure your theme in marrow.config.js</li>
            <li>Import the CSS and JavaScript files</li>
            <li>Start building with components</li>
            <li>Customize styles with utility classes</li>
          </ol>
        </div>
      </div>
    </section>

    <section>
      <span class="docs-section-label">In Context</span>
      <div class="docs-stage">
        <article class="max-w-2xl mx-auto space-y-6">
          <h1 class="font-heading text-4xl font-extrabold tracking-tight">Building Accessible Interfaces</h1>
          <p class="text-xl text-muted-foreground leading-relaxed">
            A practical guide to creating web experiences that work for everyone, from semantic markup to keyboard navigation.
          </p>
          <h2 class="font-heading text-3xl font-semibold tracking-tight border-b pb-2">Why Accessibility Matters</h2>
          <p class="leading-7">
            The web was designed to be <strong>universal</strong>. When we build with accessibility in mind, we create better experiences for <em>all</em> users, not just those with disabilities. It's a win for usability, SEO, and inclusion.
          </p>
          <blockquote class="border-l-4 border-primary pl-4 italic text-muted-foreground">
            "The power of the Web is in its universality. Access by everyone regardless of disability is an essential aspect."
            <footer class="mt-2 text-sm not-italic">&mdash; Tim Berners-Lee</footer>
          </blockquote>
          <h3 class="font-heading text-2xl font-semibold tracking-tight">Getting Started</h3>
          <p class="leading-7">
            Start by using semantic HTML elements like <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">&lt;nav&gt;</code>,
            <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">&lt;main&gt;</code>, and
            <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">&lt;article&gt;</code>. These provide built-in meaning to assistive technology.
          </p>
          <h4 class="font-heading text-xl font-semibold tracking-tight">Key Principles</h4>
          <ol class="list-decimal pl-6 space-y-1 leading-7">
            <li>Use native HTML elements whenever possible</li>
            <li>Provide text alternatives for non-text content</li>
            <li>Ensure all interactions are keyboard accessible</li>
            <li>Test with screen readers and other assistive tools</li>
          </ol>
          <p class="text-sm text-muted-foreground">Last updated: February 2026 &bull; 5 min read</p>
        </article>
      </div>
    </section>

    <section>
      <span class="docs-section-label">Code</span>
      <div class="space-y-4">
        <CodeBlock code={headingsCode} lang="html" />
        <CodeBlock code={paragraphCode} lang="html" />
        <CodeBlock code={inlineCode} lang="html" />
        <CodeBlock code={blockquoteCode} lang="html" />
        <CodeBlock code={listsCode} lang="html" />
      </div>
    </section>

    <section>
      <span class="docs-section-label">API Reference</span>
      <div class="overflow-x-auto rounded-lg border border-border">
        <table class="docs-api-table">
          <thead>
            <tr>
              <th>Class / Property</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>font-heading</code></td><td>Apply the heading font family (maps to <code>--marrow-font-heading</code>)</td></tr>
            <tr><td><code>font-body</code></td><td>Apply the body font family (maps to <code>--marrow-font-body</code>)</td></tr>
            <tr><td><code>text-muted-foreground</code></td><td>Muted text color for de-emphasized content</td></tr>
            <tr><td><code>leading-7</code></td><td>Comfortable line height (1.75rem) for body text</td></tr>
            <tr><td><code>leading-relaxed</code></td><td>Relaxed line height (1.625) for lead paragraphs</td></tr>
            <tr><td><code>tracking-tight</code></td><td>Tighter letter spacing for headings</td></tr>
          </tbody>
        </table>
      </div>
    </section>

    <section>
      <span class="docs-section-label">Guidelines</span>
      <div class="space-y-4">
        <div class="docs-note">
          <p><strong>Accessibility</strong></p>
          <ul class="list-disc list-inside mt-2 space-y-1">
            <li>Use a logical heading hierarchy (<code>h1</code> through <code>h4</code>) &mdash; never skip levels</li>
            <li>Ensure sufficient color contrast for muted text against the background</li>
            <li>Use semantic elements: <code>&lt;strong&gt;</code> for importance, <code>&lt;em&gt;</code> for emphasis, <code>&lt;blockquote&gt;</code> for quotations</li>
            <li>Link text should be descriptive &mdash; avoid generic "click here" labels</li>
            <li>Use <code>&lt;code&gt;</code> elements for inline code to convey meaning to screen readers</li>
          </ul>
        </div>
        <div class="docs-note">
          <p><strong>Best Practices</strong></p>
          <ul class="list-disc list-inside mt-2 space-y-1">
            <li>Use the dual font system: <code>font-heading</code> for headings, <code>font-body</code> for text</li>
            <li>Apply <code>tracking-tight</code> to headings for a polished feel</li>
            <li>Use lead paragraphs (<code>text-xl text-muted-foreground</code>) to introduce sections</li>
            <li>Keep line height comfortable: <code>leading-7</code> for body, <code>leading-relaxed</code> for leads</li>
            <li>Configure fonts in <code>marrow.config.js</code> and compile with <code>npx marrow-ui build</code></li>
          </ul>
        </div>
      </div>
    </section>

  </div>
</DocsLayout>
