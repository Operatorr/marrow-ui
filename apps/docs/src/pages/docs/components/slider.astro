---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";

const sliderCode = `<div x-data="mwSlider(0, 100, 50)" class="space-y-4">
  <div class="flex items-center justify-between">
    <label class="mw-label">Volume</label>
    <span class="text-sm text-muted-foreground" x-text="value + '%'"></span>
  </div>

  <div class="relative pt-1">
    <!-- Visual track -->
    <div class="mw-slider-track">
      <div class="mw-slider-range" :style="'width: ' + percentage + '%'"></div>
    </div>
    <!-- Invisible native range input -->
    <input type="range" :min="min" :max="max"
           x-model="value" @input="handleInput($event)"
           class="absolute inset-0 w-full opacity-0 cursor-pointer"
           style="margin: 0; height: 0.375rem;">
  </div>
</div>`;

const temperatureCode = `<div x-data="mwSlider(0, 50, 25)" class="space-y-4">
  <div class="flex items-center justify-between">
    <label class="mw-label">Temperature</label>
    <span class="text-sm text-muted-foreground">
      <span x-text="value"></span>&deg;C
    </span>
  </div>

  <div class="relative pt-1">
    <div class="mw-slider-track">
      <div class="mw-slider-range" :style="'width: ' + percentage + '%'"></div>
    </div>
    <input type="range" :min="min" :max="max"
           x-model="value" @input="handleInput($event)"
           class="absolute inset-0 w-full opacity-0 cursor-pointer"
           style="margin: 0; height: 0.375rem;">
  </div>

  <div class="flex justify-between text-xs text-muted-foreground">
    <span>0&deg;C</span>
    <span>25&deg;C</span>
    <span>50&deg;C</span>
  </div>
</div>`;
---

<DocsLayout title="Slider" description="A range slider input for selecting numeric values" activeComponent="slider">
  <div class="space-y-8">
    <div>
      <h1 class="font-heading text-3xl font-bold tracking-tight">Slider</h1>
      <p class="mt-2 text-lg text-muted-foreground leading-relaxed">A styled range input that lets users select a value within a defined range by dragging a thumb along a track.</p>
    </div>

    <div class="mw-separator"></div>

    <!-- Preview -->
    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight">Preview</h2>
      <div class="mw-card">
        <div x-data="mwSlider(0, 100, 50)" class="space-y-4">
          <div class="flex items-center justify-between">
            <label class="mw-label">Volume</label>
            <span class="text-sm text-muted-foreground" x-text="value + '%'"></span>
          </div>
          <div class="relative pt-1">
            <div class="mw-slider-track">
              <div class="mw-slider-range" :style="'width: ' + percentage + '%'"></div>
            </div>
            <input type="range" :min="min" :max="max" x-model="value" @input="handleInput($event)"
                   class="absolute inset-0 w-full opacity-0 cursor-pointer" style="margin: 0; height: 0.375rem;">
          </div>
        </div>
      </div>
    </div>

    <!-- Code -->
    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight">Code</h2>
      <CodeBlock code={sliderCode} lang="html" />
    </div>

    <!-- Custom Range Example -->
    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight">Custom Range</h2>
      <p class="text-muted-foreground leading-relaxed">You can customize the min/max range and display format. This example shows a temperature slider from 0°C to 50°C with tick marks.</p>
      <div class="mw-card">
        <div x-data="mwSlider(0, 50, 25)" class="space-y-4">
          <div class="flex items-center justify-between">
            <label class="mw-label">Temperature</label>
            <span class="text-sm text-muted-foreground"><span x-text="value"></span>&deg;C</span>
          </div>
          <div class="relative pt-1">
            <div class="mw-slider-track">
              <div class="mw-slider-range" :style="'width: ' + percentage + '%'"></div>
            </div>
            <input type="range" :min="min" :max="max" x-model="value" @input="handleInput($event)"
                   class="absolute inset-0 w-full opacity-0 cursor-pointer" style="margin: 0; height: 0.375rem;">
          </div>
          <div class="flex justify-between text-xs text-muted-foreground">
            <span>0&deg;C</span>
            <span>25&deg;C</span>
            <span>50&deg;C</span>
          </div>
        </div>
      </div>
      <CodeBlock code={temperatureCode} lang="html" />
    </div>

    <!-- Usage -->
    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight">Usage</h2>
      <p class="text-muted-foreground leading-relaxed">Initialize the slider with <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">x-data="mwSlider(min, max, initial)"</code>. The component manages the current value and provides a computed percentage for the visual fill bar.</p>

      <div class="space-y-4">
        <h3 class="font-heading text-lg font-semibold">Alpine.js API</h3>
        <div class="overflow-x-auto">
          <table class="w-full text-sm">
            <thead>
              <tr class="border-b">
                <th class="text-left py-2 pr-4 font-semibold">Property/Method</th>
                <th class="text-left py-2 pr-4 font-semibold">Type</th>
                <th class="text-left py-2 font-semibold">Description</th>
              </tr>
            </thead>
            <tbody class="divide-y">
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">min</td>
                <td class="py-2 pr-4 text-muted-foreground">Number</td>
                <td class="py-2">Minimum value</td>
              </tr>
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">max</td>
                <td class="py-2 pr-4 text-muted-foreground">Number</td>
                <td class="py-2">Maximum value</td>
              </tr>
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">value</td>
                <td class="py-2 pr-4 text-muted-foreground">Number</td>
                <td class="py-2">Current value (use with <code class="px-1.5 py-0.5 bg-muted rounded text-xs font-mono">x-model</code>)</td>
              </tr>
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">percentage</td>
                <td class="py-2 pr-4 text-muted-foreground">Number</td>
                <td class="py-2">Current value as percentage (0–100), computed getter</td>
              </tr>
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">handleInput(e)</td>
                <td class="py-2 pr-4 text-muted-foreground">Function</td>
                <td class="py-2">Process input events from the range element</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div class="space-y-4">
        <h3 class="font-heading text-lg font-semibold">CSS Classes</h3>
        <div class="overflow-x-auto">
          <table class="w-full text-sm">
            <thead>
              <tr class="border-b">
                <th class="text-left py-2 pr-4 font-semibold">Class</th>
                <th class="text-left py-2 font-semibold">Description</th>
              </tr>
            </thead>
            <tbody class="divide-y">
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">.mw-slider-track</td>
                <td class="py-2">The slider track container (0.375rem height, rounded, secondary background)</td>
              </tr>
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">.mw-slider-range</td>
                <td class="py-2">The filled portion of the track (absolute positioned, primary background)</td>
              </tr>
              <tr>
                <td class="py-2 pr-4 font-mono text-xs">.mw-slider-thumb</td>
                <td class="py-2">The draggable thumb handle (1.25rem circle, white background, primary border, shadow)</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div class="space-y-4">
        <h3 class="font-heading text-lg font-semibold">Accessibility</h3>
        <p class="text-muted-foreground leading-relaxed">The slider uses a native <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">&lt;input type="range"&gt;</code> element layered on top of the visual track. This ensures full keyboard support (arrow keys, Home, End, Page Up/Down) and proper screen reader announcements. The native input is transparent but maintains the correct dimensions for hit-testing.</p>
        <ul class="list-disc list-inside space-y-1 text-muted-foreground leading-relaxed">
          <li>Use a <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">&lt;label&gt;</code> element to describe the slider's purpose</li>
          <li>Consider adding <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">aria-valuetext</code> for formatted values (e.g., "$50" instead of "50")</li>
          <li>Display the current value visually for users who cannot use the slider</li>
          <li>Ensure sufficient color contrast between the track and range fill</li>
        </ul>
      </div>
    </div>
  </div>
</DocsLayout>
