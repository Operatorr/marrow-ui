---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";

const horizontalCode = `<div>
  <h3 class="font-semibold">Marrow UI</h3>
  <p class="text-sm text-muted-foreground">An open-source UI component library.</p>
</div>

<div class="mw-separator"></div>

<div class="flex gap-4 text-sm">
  <span>Documentation</span>
  <span>Components</span>
  <span>Themes</span>
</div>`;

const verticalCode = `<div class="flex items-center h-5">
  <span class="text-sm">Blog</span>
  <div class="mw-separator-vertical"></div>
  <span class="text-sm">Docs</span>
  <div class="mw-separator-vertical"></div>
  <span class="text-sm">Source</span>
</div>`;
---

<DocsLayout title="Separator" description="A visual divider to separate content sections" activeComponent="separator">
  <div class="space-y-8">
    <div>
      <h1 class="font-heading text-3xl font-bold tracking-tight">Separator</h1>
      <p class="mt-2 text-lg text-muted-foreground leading-relaxed">A horizontal or vertical line to visually divide content into distinct sections.</p>
    </div>

    <div class="mw-separator"></div>

    <!-- Preview -->
    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight">Preview</h2>

      <div>
        <p class="text-sm font-medium text-muted-foreground uppercase tracking-wider mb-3">Horizontal</p>
        <div class="mw-card">
          <div>
            <h3 class="font-heading font-semibold">Marrow UI</h3>
            <p class="text-sm text-muted-foreground">An open-source UI component library.</p>
          </div>
          <div class="mw-separator"></div>
          <div class="flex gap-4 text-sm">
            <span>Documentation</span>
            <span>Components</span>
            <span>Themes</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Vertical -->
    <div class="space-y-4">
      <h3 class="font-heading text-lg font-semibold">Vertical</h3>

      <div>
        <p class="text-sm font-medium text-muted-foreground uppercase tracking-wider mb-3">Vertical</p>
        <div class="mw-card">
          <div class="flex items-center gap-0 h-5">
            <span class="text-sm">Blog</span>
            <div class="mw-separator-vertical"></div>
            <span class="text-sm">Docs</span>
            <div class="mw-separator-vertical"></div>
            <span class="text-sm">Source</span>
            <div class="mw-separator-vertical"></div>
            <span class="text-sm">License</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Code -->
    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight">Code</h2>

      <div>
        <h3 class="font-heading text-lg font-semibold">Horizontal Separator</h3>
        <CodeBlock code={horizontalCode} lang="html" />
      </div>

      <div>
        <h3 class="font-heading text-lg font-semibold">Vertical Separator</h3>
        <CodeBlock code={verticalCode} lang="html" />
      </div>
    </div>

    <!-- Usage -->
    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight">Usage</h2>

      <p class="text-muted-foreground leading-relaxed">
        Use <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-separator</code> for a horizontal divider that spans the full width with vertical margin. Use <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-separator-vertical</code> for a vertical divider inside a flex container — make sure the parent has a set height (e.g., <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">h-5</code>) so the line is visible.
      </p>

      <div class="space-y-4">
        <h3 class="font-heading text-lg font-semibold">CSS Classes</h3>
        <div class="mw-card overflow-x-auto">
          <table class="w-full text-sm">
            <thead>
              <tr class="border-b border-border">
                <th class="text-left py-2 px-4 font-semibold">Class</th>
                <th class="text-left py-2 px-4 font-semibold">Description</th>
              </tr>
            </thead>
            <tbody>
              <tr class="border-b border-border">
                <td class="py-2 px-4 font-mono text-sm">.mw-separator</td>
                <td class="py-2 px-4 text-muted-foreground">Horizontal separator (1px height, full width, 1rem vertical margin)</td>
              </tr>
              <tr>
                <td class="py-2 px-4 font-mono text-sm">.mw-separator-vertical</td>
                <td class="py-2 px-4 text-muted-foreground">Vertical separator (1px width, full height, 0.5rem horizontal margin)</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div class="space-y-4">
        <h3 class="font-heading text-lg font-semibold">Accessibility</h3>
        <p class="text-muted-foreground leading-relaxed">
          For purely decorative separators, no additional ARIA attributes are needed. If the separator serves a semantic purpose (e.g., dividing major sections of content), add <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">role="separator"</code> to improve screen reader experience.
        </p>
        <p class="text-muted-foreground leading-relaxed">
          No JavaScript is required. The separator color is controlled by the <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">--marrow-border</code> CSS variable.
        </p>
      </div>
    </div>
  </div>
</DocsLayout>
