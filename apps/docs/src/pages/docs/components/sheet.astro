---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";

const sheetCode = `<div x-data="mwSheet()">
  <button @click="show()" class="mw-btn mw-btn-outline">Open Sheet</button>

  <template x-if="open">
    <div>
      <!-- Overlay -->
      <div class="mw-overlay" @click="close()"
           x-transition:enter="transition ease-out duration-300"
           x-transition:enter-start="opacity-0"
           x-transition:enter-end="opacity-100"
           x-transition:leave="transition ease-in duration-200"
           x-transition:leave-start="opacity-100"
           x-transition:leave-end="opacity-0"></div>

      <!-- Sheet Panel (right side) -->
      <div class="mw-sheet mw-sheet-right"
           x-transition:enter="transition ease-out duration-300"
           x-transition:enter-start="translate-x-full"
           x-transition:enter-end="translate-x-0"
           x-transition:leave="transition ease-in duration-200"
           x-transition:leave-start="translate-x-0"
           x-transition:leave-end="translate-x-full">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h3 class="font-heading text-lg font-semibold">Sheet Title</h3>
            <p class="text-sm text-muted-foreground">Sheet description.</p>
          </div>
          <button @click="close()" class="mw-btn mw-btn-ghost mw-btn-icon">
            &times;
          </button>
        </div>
        <div class="mw-separator"></div>
        <!-- Sheet content here -->
      </div>
    </div>
  </template>
</div>`;
---

<DocsLayout title="Sheet" description="A side panel that slides in from the edge of the screen" activeComponent="sheet">
      <div class="space-y-8">
        <div>
          <h1 class="font-heading text-3xl font-bold tracking-tight">Sheet</h1>
          <p class="mt-2 text-lg text-muted-foreground">A panel that slides in from the edge of the viewport, useful for navigation, forms, or supplementary content.</p>
        </div>

        <div class="mw-separator"></div>

        <!-- Preview -->
        <div class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Preview</h2>
          <div class="mw-card">
            <div x-data="mwSheet()">
              <button @click="show()" class="mw-btn mw-btn-outline">Open Sheet</button>

              <!-- Overlay -->
              <template x-if="open">
                <div>
                  <div class="mw-overlay" @click="close()"
                       x-transition:enter="transition ease-out duration-300"
                       x-transition:enter-start="opacity-0"
                       x-transition:enter-end="opacity-100"
                       x-transition:leave="transition ease-in duration-200"
                       x-transition:leave-start="opacity-100"
                       x-transition:leave-end="opacity-0"></div>

                  <!-- Sheet Panel -->
                  <div class="mw-sheet mw-sheet-right"
                       x-transition:enter="transition ease-out duration-300"
                       x-transition:enter-start="translate-x-full"
                       x-transition:enter-end="translate-x-0"
                       x-transition:leave="transition ease-in duration-200"
                       x-transition:leave-start="translate-x-0"
                       x-transition:leave-end="translate-x-full">
                    <div class="flex items-center justify-between mb-4">
                      <div>
                        <h3 class="font-heading text-lg font-semibold">Edit Profile</h3>
                        <p class="text-sm text-muted-foreground">Make changes to your profile here.</p>
                      </div>
                      <button @click="close()" class="mw-btn mw-btn-ghost mw-btn-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
                      </button>
                    </div>
                    <div class="mw-separator"></div>
                    <div class="space-y-4 mt-4">
                      <div>
                        <label class="mw-label">Name</label>
                        <input type="text" class="mw-input mt-1.5" placeholder="Your name" value="John Doe">
                      </div>
                      <div>
                        <label class="mw-label">Email</label>
                        <input type="email" class="mw-input mt-1.5" placeholder="you@example.com" value="john@example.com">
                      </div>
                      <div>
                        <label class="mw-label">Bio</label>
                        <textarea class="mw-textarea mt-1.5" placeholder="Tell us about yourself"></textarea>
                      </div>
                    </div>
                    <div class="flex justify-end gap-2 mt-6">
                      <button @click="close()" class="mw-btn mw-btn-outline">Cancel</button>
                      <button @click="close()" class="mw-btn mw-btn-primary">Save Changes</button>
                    </div>
                  </div>
                </div>
              </template>
            </div>
          </div>
        </div>

        <!-- Code -->
        <div class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Code</h2>
          <CodeBlock code={sheetCode} lang="html" />
        </div>

        <!-- Usage Notes -->
        <div class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Usage</h2>
          <p class="text-muted-foreground leading-relaxed">The sheet uses <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">x-data="mwSheet()"</code> with <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">show()</code> and <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">close()</code> methods. Combine <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-sheet</code> with a direction class:</p>

          <div class="mw-card">
            <h3 class="font-heading text-base font-semibold mb-3">Alpine.js API</h3>
            <div class="overflow-x-auto">
              <table class="w-full text-sm">
                <thead>
                  <tr class="border-b border-border">
                    <th class="text-left py-2 pr-4 font-semibold">Property/Method</th>
                    <th class="text-left py-2 pr-4 font-semibold">Type</th>
                    <th class="text-left py-2 font-semibold">Description</th>
                  </tr>
                </thead>
                <tbody class="divide-y divide-border">
                  <tr>
                    <td class="py-2 pr-4"><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">open</code></td>
                    <td class="py-2 pr-4 text-muted-foreground">Boolean</td>
                    <td class="py-2 text-muted-foreground">Sheet visibility state</td>
                  </tr>
                  <tr>
                    <td class="py-2 pr-4"><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">show()</code></td>
                    <td class="py-2 pr-4 text-muted-foreground">Method</td>
                    <td class="py-2 text-muted-foreground">Open the sheet</td>
                  </tr>
                  <tr>
                    <td class="py-2 pr-4"><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">close()</code></td>
                    <td class="py-2 pr-4 text-muted-foreground">Method</td>
                    <td class="py-2 text-muted-foreground">Close the sheet</td>
                  </tr>
                </tbody>
              </table>
            </div>
          </div>

          <div class="mw-card">
            <h3 class="font-heading text-base font-semibold mb-3">CSS Classes</h3>
            <ul class="list-disc pl-6 space-y-2 text-muted-foreground">
              <li><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-sheet</code> &mdash; base panel (fixed, z-51, bg, border, shadow, padding)</li>
              <li><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-sheet-right</code> &mdash; slides in from the right (24rem width)</li>
              <li><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-sheet-left</code> &mdash; slides in from the left</li>
              <li><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-sheet-top</code> &mdash; slides down from the top</li>
              <li><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-sheet-bottom</code> &mdash; slides up from the bottom</li>
              <li><code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">.mw-overlay</code> &mdash; backdrop (closes sheet on click)</li>
            </ul>
          </div>
        </div>

        <!-- Accessibility -->
        <div class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Accessibility</h2>
          <div class="mw-card space-y-3">
            <p class="text-muted-foreground leading-relaxed">The sheet component follows accessibility best practices:</p>
            <ul class="list-disc pl-6 space-y-2 text-muted-foreground">
              <li><strong>Role:</strong> Add <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">role="dialog"</code> to the sheet panel for proper semantic markup</li>
              <li><strong>Modal state:</strong> Add <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">aria-modal="true"</code> to indicate the sheet is modal</li>
              <li><strong>Focus management:</strong> When the sheet opens, focus should move to the first interactive element or the sheet itself. On close, focus should return to the trigger button</li>
              <li><strong>Keyboard navigation:</strong> Pressing <kbd class="px-2 py-1 bg-muted rounded text-sm font-mono border border-border">Escape</kbd> should close the sheet. Use <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">@keydown.escape="close()"</code> on the sheet panel</li>
              <li><strong>Screen readers:</strong> Use <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">aria-labelledby</code> on the sheet panel to reference the sheet title heading</li>
              <li><strong>Backdrop:</strong> The overlay backdrop provides a clear visual indication of modal state and allows users to dismiss by clicking outside</li>
            </ul>
          </div>
        </div>
      </div>

</DocsLayout>
