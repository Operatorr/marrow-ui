---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout title="Resizable" description="Resizable panels with a draggable divider" activeComponent="resizable">
      <div class="space-y-8">
        <div>
          <h1 class="font-heading text-3xl font-bold tracking-tight">Resizable</h1>
          <p class="mt-2 text-lg text-muted-foreground">Resizable panels let users adjust the layout by dragging a divider between two panes.</p>
        </div>

        <div class="mw-separator"></div>

        <!-- Preview -->
        <div class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Preview</h2>
          <div class="mw-card p-0 overflow-hidden">
            <div x-data="mwResizable(50)" class="flex h-64 select-none" style="min-height: 16rem;">
              <!-- Left Panel -->
              <div class="overflow-auto p-4" :style="'width:' + split + '%'">
                <h3 class="font-heading font-semibold mb-2">Left Panel</h3>
                <p class="text-sm text-muted-foreground">This panel is resizable. Drag the handle to adjust the width. The split is currently at <span x-text="Math.round(split)"></span>%.</p>
              </div>
              <!-- Drag Handle -->
              <div class="mw-resizable-handle" @mousedown="startDrag($event)" @touchstart.prevent="startDrag($event)"></div>
              <!-- Right Panel -->
              <div class="overflow-auto p-4 flex-1">
                <h3 class="font-heading font-semibold mb-2">Right Panel</h3>
                <p class="text-sm text-muted-foreground">This panel fills the remaining space. Try dragging the divider left and right.</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Code -->
        <div class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Code</h2>
          <div class="mw-code-block">
            <button onclick="copyCode(this)" class="copy-btn" title="Copy to clipboard">
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
            </button>
            <pre><code>&lt;div x-data="mwResizable(50)" class="flex h-64 select-none border rounded-md overflow-hidden"&gt;
  &lt;!-- Left Panel --&gt;
  &lt;div class="overflow-auto p-4" :style="'width:' + split + '%'"&gt;
    &lt;h3 class="font-semibold mb-2"&gt;Left Panel&lt;/h3&gt;
    &lt;p class="text-sm text-muted-foreground"&gt;Resizable content here.&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- Drag Handle --&gt;
  &lt;div class="mw-resizable-handle"
       @mousedown="startDrag($event)"
       @touchstart.prevent="startDrag($event)"&gt;&lt;/div&gt;

  &lt;!-- Right Panel --&gt;
  &lt;div class="overflow-auto p-4 flex-1"&gt;
    &lt;h3 class="font-semibold mb-2"&gt;Right Panel&lt;/h3&gt;
    &lt;p class="text-sm text-muted-foreground"&gt;This fills the remaining space.&lt;/p&gt;
  &lt;/div&gt;
&lt;/div&gt;</code></pre>
          </div>
        </div>

        <!-- Usage Notes -->
        <div class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Usage</h2>
          <p class="text-muted-foreground leading-relaxed">The <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">mwResizable(50)</code> function accepts an initial split percentage (0&ndash;100). The left panel width is bound to the <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">split</code> value, and the right panel uses <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">flex-1</code> to fill the rest.</p>
          <p class="text-muted-foreground leading-relaxed">The drag handle constrains values between 10% and 90% to prevent panels from collapsing entirely. Both mouse and touch events are supported.</p>
        </div>
      </div>

</DocsLayout>
