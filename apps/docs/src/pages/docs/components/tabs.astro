---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";

const tabsCode = `<div x-data="mwTabs('tab1')">
  <!-- Tab triggers -->
  <div class="mw-tabs-list">
    <button class="mw-tab-trigger"
            :data-state="isActive('tab1') ? 'active' : ''"
            @click="setActive('tab1')">Account</button>
    <button class="mw-tab-trigger"
            :data-state="isActive('tab2') ? 'active' : ''"
            @click="setActive('tab2')">Password</button>
    <button class="mw-tab-trigger"
            :data-state="isActive('tab3') ? 'active' : ''"
            @click="setActive('tab3')">Notifications</button>
  </div>

  <!-- Tab panels -->
  <div class="mt-4">
    <div x-show="isActive('tab1')">
      <h3 class="font-heading font-semibold">Account Settings</h3>
      <p class="text-sm text-muted-foreground">Update your account information.</p>
      <!-- Panel content -->
    </div>

    <div x-show="isActive('tab2')" x-cloak>
      <h3 class="font-heading font-semibold">Change Password</h3>
      <p class="text-sm text-muted-foreground">Update your password.</p>
      <!-- Panel content -->
    </div>

    <div x-show="isActive('tab3')" x-cloak>
      <h3 class="font-heading font-semibold">Notification Preferences</h3>
      <p class="text-sm text-muted-foreground">Choose how you want to be notified.</p>
      <!-- Panel content -->
    </div>
  </div>
</div>`;
---

<DocsLayout title="Tabs" description="Tabbed navigation for switching between content panels" activeComponent="tabs">
      <div class="space-y-8">
        <div>
          <h1 class="font-heading text-3xl font-bold tracking-tight text-pretty">Tabs</h1>
          <p class="mt-2 text-lg text-muted-foreground">A set of layered content sections, displayed one at a time via tabbed navigation.</p>
        </div>

        <div class="mw-separator"></div>

        <!-- Preview -->
        <section class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Preview</h2>
          <div class="mw-card p-6">
            <div x-data="mwTabs('tab1')">
              <!-- Tab triggers -->
              <div class="mw-tabs-list">
                <button class="mw-tab-trigger"
                        :data-state="isActive('tab1') ? 'active' : ''"
                        @click="setActive('tab1')">Account</button>
                <button class="mw-tab-trigger"
                        :data-state="isActive('tab2') ? 'active' : ''"
                        @click="setActive('tab2')">Password</button>
                <button class="mw-tab-trigger"
                        :data-state="isActive('tab3') ? 'active' : ''"
                        @click="setActive('tab3')">Notifications</button>
              </div>

              <!-- Tab panels -->
              <div class="mt-4">
                <div x-show="isActive('tab1')" class="space-y-4">
                  <h3 class="font-heading font-semibold">Account Settings</h3>
                  <p class="text-sm text-muted-foreground">Update your account information and preferences.</p>
                  <div class="space-y-2">
                    <label class="mw-label">Name</label>
                    <input type="text" class="mw-input max-w-sm" placeholder="Your name" value="John Doe">
                  </div>
                  <div class="space-y-2">
                    <label class="mw-label">Email</label>
                    <input type="email" class="mw-input max-w-sm" placeholder="Your email" value="john@example.com">
                  </div>
                </div>

                <div x-show="isActive('tab2')" x-cloak class="space-y-4">
                  <h3 class="font-heading font-semibold">Change Password</h3>
                  <p class="text-sm text-muted-foreground">Update your password to keep your account secure.</p>
                  <div class="space-y-2">
                    <label class="mw-label">Current Password</label>
                    <input type="password" class="mw-input max-w-sm" placeholder="Current password">
                  </div>
                  <div class="space-y-2">
                    <label class="mw-label">New Password</label>
                    <input type="password" class="mw-input max-w-sm" placeholder="New password">
                  </div>
                </div>

                <div x-show="isActive('tab3')" x-cloak class="space-y-4">
                  <h3 class="font-heading font-semibold">Notification Preferences</h3>
                  <p class="text-sm text-muted-foreground">Choose how you want to be notified.</p>
                  <div class="flex items-center gap-3" x-data="mwSwitch(true)">
                    <button class="mw-switch"
                            :data-state="checked ? 'checked' : 'unchecked'"
                            @click="toggle()"
                            role="switch"
                            :aria-checked="checked.toString()">
                      <span class="mw-switch-thumb"></span>
                    </button>
                    <span class="text-sm">Email notifications</span>
                  </div>
                  <div class="flex items-center gap-3" x-data="mwSwitch()">
                    <button class="mw-switch"
                            :data-state="checked ? 'checked' : 'unchecked'"
                            @click="toggle()"
                            role="switch"
                            :aria-checked="checked.toString()">
                      <span class="mw-switch-thumb"></span>
                    </button>
                    <span class="text-sm">Push notifications</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Code -->
        <section class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Code</h2>
          <CodeBlock code={tabsCode} lang="html" />
        </section>

        <!-- Usage -->
        <section class="space-y-4">
          <h2 class="font-heading text-2xl font-semibold tracking-tight">Usage</h2>

          <div class="space-y-6">
            <div>
              <h3 class="font-heading text-lg font-semibold mb-3">Alpine.js API</h3>
              <p class="text-sm text-muted-foreground mb-4">Initialize with <code class="mw-code">x-data="mwTabs(defaultTab)"</code></p>
              <div class="mw-card overflow-hidden">
                <table class="w-full text-sm">
                  <thead class="border-b border-border bg-muted/50">
                    <tr>
                      <th class="text-left p-3 font-semibold">Property/Method</th>
                      <th class="text-left p-3 font-semibold">Type</th>
                      <th class="text-left p-3 font-semibold">Description</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-border">
                    <tr>
                      <td class="p-3"><code class="mw-code">active</code></td>
                      <td class="p-3">String</td>
                      <td class="p-3">Currently active tab ID</td>
                    </tr>
                    <tr>
                      <td class="p-3"><code class="mw-code">setActive(tab)</code></td>
                      <td class="p-3">Function</td>
                      <td class="p-3">Switch to the specified tab</td>
                    </tr>
                    <tr>
                      <td class="p-3"><code class="mw-code">isActive(tab)</code></td>
                      <td class="p-3">Function</td>
                      <td class="p-3">Check if the specified tab is active (returns boolean)</td>
                    </tr>
                  </tbody>
                </table>
              </div>
            </div>

            <div>
              <h3 class="font-heading text-lg font-semibold mb-3">CSS Classes</h3>
              <div class="mw-card overflow-hidden">
                <table class="w-full text-sm">
                  <thead class="border-b border-border bg-muted/50">
                    <tr>
                      <th class="text-left p-3 font-semibold">Class</th>
                      <th class="text-left p-3 font-semibold">Description</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-border">
                    <tr>
                      <td class="p-3"><code class="mw-code">mw-tabs-list</code></td>
                      <td class="p-3">Container for tab triggers with inline-flex layout, muted background, and rounded corners</td>
                    </tr>
                    <tr>
                      <td class="p-3"><code class="mw-code">mw-tab-trigger</code></td>
                      <td class="p-3">Individual tab button with transitions and muted-foreground color</td>
                    </tr>
                    <tr>
                      <td class="p-3"><code class="mw-code">mw-tab-trigger[data-state="active"]</code></td>
                      <td class="p-3">Active tab state with white background, foreground color, and shadow</td>
                    </tr>
                  </tbody>
                </table>
              </div>
            </div>

            <div>
              <h3 class="font-heading text-lg font-semibold mb-3">Accessibility</h3>
              <ul class="space-y-2 text-sm text-muted-foreground list-disc list-inside">
                <li>Tab triggers are keyboard navigable with arrow keys when using proper ARIA attributes</li>
                <li>Use <code class="mw-code">x-cloak</code> on hidden tab panels to prevent content flash on load</li>
                <li>Tab panels automatically hide/show with Alpine.js <code class="mw-code">x-show</code> directive</li>
                <li>Consider adding <code class="mw-code">role="tablist"</code>, <code class="mw-code">role="tab"</code>, and <code class="mw-code">role="tabpanel"</code> for enhanced screen reader support</li>
                <li>Add <code class="mw-code">aria-selected</code> to tab triggers to indicate active state</li>
              </ul>
            </div>
          </div>
        </section>
      </div>
</DocsLayout>
