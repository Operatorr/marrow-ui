---
import DocsLayout from "../../../layouts/DocsLayout.astro";
import CodeBlock from "../../../components/CodeBlock.astro";

const datePickerCode = `<div x-data="mwDatePicker()">
  <label class="block text-sm font-medium mb-2">Event Date</label>
  <div class="relative">
    <!-- Trigger Button -->
    <button
      @click="toggle()"
      type="button"
      class="mw-input flex items-center justify-between text-left cursor-pointer w-full">
      <span :class="selected ? 'text-foreground' : 'text-muted-foreground'"
        x-text="formatted"></span>
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16"
        viewBox="0 0 24 24" fill="none" stroke="currentColor"
        stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
        class="text-muted-foreground shrink-0" aria-hidden="true">
        <rect width="18" height="18" x="3" y="4" rx="2" ry="2"/>
        <line x1="16" x2="16" y1="2" y2="6"/>
        <line x1="8" x2="8" y1="2" y2="6"/>
        <line x1="3" x2="21" y1="10" y2="10"/>
      </svg>
    </button>

    <!-- Calendar Dropdown -->
    <div x-show="open" @click.away="open = false" x-cloak
      class="absolute z-10 mt-1 mw-popover p-3 w-[280px]"
      x-transition:enter="transition-opacity transition-transform duration-100 ease-out"
      x-transition:enter-start="opacity-0 -translate-y-1"
      x-transition:enter-end="opacity-100 translate-y-0"
      x-transition:leave="transition-opacity transition-transform duration-75 ease-in"
      x-transition:leave-start="opacity-100 translate-y-0"
      x-transition:leave-end="opacity-0 -translate-y-1">

      <!-- Month Navigation -->
      <div class="flex items-center justify-between mb-3">
        <button @click="prevMonth()" type="button"
          class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm"
          aria-label="Previous month">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14"
            viewBox="0 0 24 24" fill="none" stroke="currentColor"
            stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m15 18-6-6 6-6"/>
          </svg>
        </button>
        <span class="text-sm font-medium" x-text="monthName + ' ' + currentYear"></span>
        <button @click="nextMonth()" type="button"
          class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm"
          aria-label="Next month">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14"
            viewBox="0 0 24 24" fill="none" stroke="currentColor"
            stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m9 18 6-6-6-6"/>
          </svg>
        </button>
      </div>

      <!-- Day Labels -->
      <div class="grid grid-cols-7 gap-0 mb-1">
        <template x-for="day in ['Su', 'Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa']">
          <div class="text-center text-xs text-muted-foreground py-1" x-text="day"></div>
        </template>
      </div>

      <!-- Day Grid -->
      <div class="grid grid-cols-7 gap-0">
        <template x-for="(day, index) in days" :key="index">
          <button
            @click="selectDate(day)"
            type="button"
            :disabled="!day"
            :class="{
              'bg-primary text-primary-foreground': isSelected(day),
              'hover:bg-accent': day && !isSelected(day),
              'invisible': !day
            }"
            class="w-8 h-8 text-sm rounded-marrow flex items-center justify-center transition-colors"
            x-text="day || ''">
          </button>
        </template>
      </div>
    </div>
  </div>
</div>`;
---

<DocsLayout title="Date Picker" description="An input with a calendar dropdown for selecting dates" activeComponent="date-picker">
  <div class="space-y-8">
    <div>
      <h1 class="font-heading text-3xl font-bold tracking-tight text-pretty">Date Picker</h1>
      <p class="mt-2 text-muted-foreground leading-relaxed">An input with a calendar dropdown for selecting dates. Uses Alpine.js for interactive state management and keyboard navigation.</p>
    </div>

    <div class="mw-separator"></div>

    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight text-pretty">Preview</h2>
      <div class="mw-card flex items-center justify-center min-h-[420px]">
        <div class="w-full max-w-xs" x-data="mwDatePicker()">
          <label class="block text-sm font-medium mb-2">Event Date</label>
          <div class="relative">
            <button
              @click="toggle()"
              type="button"
              class="mw-input flex items-center justify-between text-left cursor-pointer w-full">
              <span :class="selected ? 'text-foreground' : 'text-muted-foreground'" x-text="formatted"></span>
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-muted-foreground shrink-0" aria-hidden="true"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
            </button>

            <!-- Calendar Dropdown -->
            <div x-show="open" @click.away="open = false" x-cloak
              class="absolute z-10 mt-1 mw-popover p-3 w-[280px]"
              x-transition:enter="transition-opacity transition-transform duration-100 ease-out"
              x-transition:enter-start="opacity-0 -translate-y-1"
              x-transition:enter-end="opacity-100 translate-y-0"
              x-transition:leave="transition-opacity transition-transform duration-75 ease-in"
              x-transition:leave-start="opacity-100 translate-y-0"
              x-transition:leave-end="opacity-0 -translate-y-1">

              <!-- Month Navigation -->
              <div class="flex items-center justify-between mb-3">
                <button @click="prevMonth()" type="button"
                  class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm"
                  aria-label="Previous month">
                  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
                </button>
                <span class="text-sm font-medium" x-text="monthName + ' ' + currentYear"></span>
                <button @click="nextMonth()" type="button"
                  class="mw-btn mw-btn-ghost mw-btn-icon mw-btn-sm"
                  aria-label="Next month">
                  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
                </button>
              </div>

              <!-- Day Labels -->
              <div class="grid grid-cols-7 gap-0 mb-1">
                <template x-for="day in ['Su', 'Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa']">
                  <div class="text-center text-xs text-muted-foreground py-1" x-text="day"></div>
                </template>
              </div>

              <!-- Day Grid -->
              <div class="grid grid-cols-7 gap-0">
                <template x-for="(day, index) in days" x-bind:key="index">
                  <button
                    @click="selectDate(day)"
                    type="button"
                    :disabled="!day"
                    :class="{
                      'bg-primary text-primary-foreground': isSelected(day),
                      'hover:bg-accent': day && !isSelected(day),
                      'invisible': !day
                    }"
                    class="w-8 h-8 text-sm rounded-marrow flex items-center justify-center transition-colors"
                    x-text="day || ''">
                  </button>
                </template>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight text-pretty">Code</h2>
      <CodeBlock code={datePickerCode} lang="html" />
    </div>

    <div class="space-y-4">
      <h2 class="font-heading text-2xl font-semibold tracking-tight text-pretty">Usage</h2>

      <div class="space-y-4">
        <div>
          <h3 class="font-heading text-lg font-semibold mb-3">Alpine.js API</h3>
          <p class="text-muted-foreground leading-relaxed mb-4">
            The date picker uses <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">mwDatePicker()</code> which provides the following reactive properties and methods:
          </p>

          <div class="mw-card overflow-hidden">
            <table class="w-full text-sm">
              <thead class="border-b border-border">
                <tr class="bg-muted/50">
                  <th class="text-left p-3 font-semibold">Property/Method</th>
                  <th class="text-left p-3 font-semibold">Type</th>
                  <th class="text-left p-3 font-semibold">Description</th>
                </tr>
              </thead>
              <tbody class="divide-y divide-border">
                <tr>
                  <td class="p-3 font-mono text-xs">open</td>
                  <td class="p-3 text-muted-foreground">Boolean</td>
                  <td class="p-3">Calendar dropdown visibility state</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">selected</td>
                  <td class="p-3 text-muted-foreground">Date | null</td>
                  <td class="p-3">Currently selected date object</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">currentMonth</td>
                  <td class="p-3 text-muted-foreground">Number</td>
                  <td class="p-3">Current displayed month (0-11)</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">currentYear</td>
                  <td class="p-3 text-muted-foreground">Number</td>
                  <td class="p-3">Current displayed year</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">monthName</td>
                  <td class="p-3 text-muted-foreground">String (getter)</td>
                  <td class="p-3">Formatted month name (e.g., "January")</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">formatted</td>
                  <td class="p-3 text-muted-foreground">String (getter)</td>
                  <td class="p-3">Display string: "MMM DD, YYYY" or "Pick a date"</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">days</td>
                  <td class="p-3 text-muted-foreground">Array (getter)</td>
                  <td class="p-3">Calendar grid with day numbers and padding</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">toggle()</td>
                  <td class="p-3 text-muted-foreground">Function</td>
                  <td class="p-3">Toggle dropdown open/closed</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">selectDate(day)</td>
                  <td class="p-3 text-muted-foreground">Function</td>
                  <td class="p-3">Select a date and close dropdown</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">isSelected(day)</td>
                  <td class="p-3 text-muted-foreground">Function</td>
                  <td class="p-3">Check if day is selected (returns boolean)</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">prevMonth()</td>
                  <td class="p-3 text-muted-foreground">Function</td>
                  <td class="p-3">Navigate to previous month</td>
                </tr>
                <tr>
                  <td class="p-3 font-mono text-xs">nextMonth()</td>
                  <td class="p-3 text-muted-foreground">Function</td>
                  <td class="p-3">Navigate to next month</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>

        <div>
          <h3 class="font-heading text-lg font-semibold mb-3">Accessibility</h3>
          <ul class="space-y-2 text-muted-foreground leading-relaxed">
            <li class="flex gap-2">
              <span class="text-primary shrink-0">•</span>
              <span>Add <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">aria-label</code> attributes to navigation buttons for screen readers</span>
            </li>
            <li class="flex gap-2">
              <span class="text-primary shrink-0">•</span>
              <span>Add <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">aria-hidden="true"</code> to decorative calendar icon</span>
            </li>
            <li class="flex gap-2">
              <span class="text-primary shrink-0">•</span>
              <span>Include <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">type="button"</code> on all buttons to prevent form submission</span>
            </li>
            <li class="flex gap-2">
              <span class="text-primary shrink-0">•</span>
              <span>Day buttons are disabled when empty using <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">:disabled="!day"</code></span>
            </li>
            <li class="flex gap-2">
              <span class="text-primary shrink-0">•</span>
              <span>Selected date is visually distinguished with high contrast colors</span>
            </li>
            <li class="flex gap-2">
              <span class="text-primary shrink-0">•</span>
              <span>Click-away behavior allows closing with <kbd class="px-1.5 py-0.5 bg-muted rounded text-xs font-mono">Esc</kbd> or outside click</span>
            </li>
          </ul>
        </div>

        <div>
          <h3 class="font-heading text-lg font-semibold mb-3">Form Integration</h3>
          <p class="text-muted-foreground leading-relaxed">
            To use the date picker in a form, add a hidden input field that syncs with the <code class="px-1.5 py-0.5 bg-muted rounded text-sm font-mono">selected</code> date:
          </p>
          <div class="mw-card mt-3">
            <pre class="text-sm"><code>&lt;input type="hidden" name="event_date" :value="selected?.toISOString()" /&gt;</code></pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</DocsLayout>
